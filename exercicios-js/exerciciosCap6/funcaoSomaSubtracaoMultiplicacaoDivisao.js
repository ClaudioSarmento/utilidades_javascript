const operacoes = (num1, num2) => {
    return console.log(`
        soma = ${num1 + num2}
        subtracao = ${num1 - num2}
        multiplicação = ${num1 * num2}
        divisão = ${num1 / num2}
    `)
}

operacoes(100, 10)